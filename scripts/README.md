# Scripts

Scripts will be added later.

Do not add dependencies or a runtime until the manual workflow has been tested.

Future scripts may include:

- create project
- extract frames
- generate task files
- collect prompt pack
- package outputs
- validate project folder structure

Scripts should stay small and local-first. They should support the workflow, not replace the project files as the source of truth.
